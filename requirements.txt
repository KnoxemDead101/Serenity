# Web framework: turns Python functions into API routes (URLs).
fastapi>=0.115
# The server that runs the FastAPI app.
uvicorn>=0.30
# Maps Python classes to database tables (the ORM).
sqlalchemy>=2.0
# Testing
pytest>=8.0
httpx>=0.27  # used by FastAPI's TestClient
